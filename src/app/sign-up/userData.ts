import { Title } from '@angular/platform-browser';

export interface userData {
    username?: string,
    password?: string,
    firstName?: string,
    lastName?: string,
    Date?: any,
    Time?:any,
    gender?: boolean,
    city?: string,
    address?: string,
    email?: string,
    role?: string
};