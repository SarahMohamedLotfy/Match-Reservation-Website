import { Title } from '@angular/platform-browser';

export interface userData {
    username?: string,
    userPassword?: string,
    firstName?: string,
    lastName?: string,
    Date?: any,
    Time?:any,
    gender?: string,
    city?: string,
    address?: string,
    email?: string,
    role?: string
};